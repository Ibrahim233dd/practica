#include <stdio.h>

int main() {
   int acumulador = 0, valor, valor1, valor2, valor3, valor4, cit = 0 ;
   
   scanf("%i", &valor);
   while( valor != 0 && cit = 0 ) {
      acumulador = acumulador + valor;
      scanf("%i", &valor1);
      cit++;
   }
   cit = 0;
   scanf("%i", &valor);
   printf("%i\n", acumulador);
   
   while( valor1 != 0 && cit = 1 ) {
      acumulador = acumulador + valor1;
      scanf("%i", &valor2);
      cit++;
   }
   cit = 1;
   scanf("%i", &valor1);
   printf("%i\n", acumulador);
   
   while( valor2 != 0 ) {
      acumulador = acumulador + valor2;
      scanf("%i", &valor3);
      cit++;
   }
   cit = 2;
   scanf("%i", &valor2);
   printf("%i\n", acumulador);
   while( valor3 != 0 ) {
      acumulador = acumulador + valor3;
      scanf("%i", &valor4);
      cit++;
   }
   cit = 0;
   scanf("%i", &valor);
   printf("%i\n", acumulador);
   while( valor4 != 0 ) {
      acumulador = acumulador + valor4;
      scanf("%i", &valor5);
   }
   cit = 0;
   scanf("%i", &valor);
   printf("%i\n", acumulador);
   while( valor5 != 0 ) {
      acumulador = acumulador + valor5;
      printf("%i\n", acumulador)
   }
   cit = 0;
   scanf("%i", &valor);
   printf("%i\n", acumulador);
   ;}
     



